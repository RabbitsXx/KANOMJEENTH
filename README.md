# Kanomjeen Server Suite

Custom RocketModFix/LDM plugin suite and Workshop UI source for the **Kanomjeen** Unturned server.

> **AI agents and new contributors: read [`AGENTS.md`](AGENTS.md) first.** It documents the toolchain, the Unity/Workshop export workflow, verification commands, known traps and the frozen identities.

## Target
- Game: Unturned Dedicated Server
- Map: California 2
- Style: Semi-Vanilla Survival PvP
- Framework: RocketModFix / LDM
- Language: C#
- Runtime target: `netstandard2.1`
- Reference packages:
  - `RocketModFix.LDM.Redist 4.9.3.18`
  - `RocketModFix.Unturned.Redist.Server 3.26.3.11`
  - `RocketModFix.UnityEngine.Redist 2022.3.62.3`

## Implemented source modules
1. **Kanomjeen.Core** — combat/raid state, restricted zones, persistent cooldowns, shared cross-feature teleport lock, player resolver, command rate limiting, shared GUI bridge.
2. **Kanomjeen.TPA** — `/tpa`, `/tpahere`, accept/deny/cancel/toggle, warmup, combat/raid/zone restrictions, GUI request handling.
3. **Kanomjeen.Homes** — persistent named homes, limits, teleport cooldown/warmup, GUI list.
4. **Kanomjeen.Kits** — config-driven kits with persistent cooldowns and starter-survival default.
5. **Kanomjeen.Respawn** — short spawn protection ending on movement/attack/expiry.
6. **Kanomjeen.BuildGuard** — placement-time zone/item/type/owner limits without per-frame world scans.
7. **Kanomjeen.Airdrops** — config-driven California 2 spawn points, automatic scheduling, dynamic PvP objective zones and GUI status.
8. **Kanomjeen.Stats** — persistent kills, deaths, KDR, fatal headshots, zombies, playtime and longest life.
9. **Kanomjeen.ServerManager** — autosave, restart countdown, safe save/shutdown and announcements.
10. **Kanomjeen.AdminAudit** — warn/mute/kick/ban/unban/inspect/god/vanish plus buffered JSONL audit logging.
11. **Kanomjeen.VehicleGuard** — lightweight vehicle inspection and event logging. No virtual garage.

## Shared design rules
- New permissions use only `kanomjeen.*`.
- No public `/warp`.
- No unrestricted virtual vault or virtual garage at launch.
- No broad item/vehicle economy shop at launch.
- No VIP combat/raid bypass.
- No end-game starter kit.
- TPA/Home revalidate combat, raid, vehicle, restricted zones and destination immediately before teleport.
- California 2 locations are configured as data; map locations are not hardcoded into gameplay code.
- Airdrops create dynamic objective zones so TPA/Home/Kit cannot trivialize a live PvP objective.
- Persistent data uses batched XML/JSONL writes rather than synchronous writes on every command.

## GUI / Steam Workshop source
`workshop-ui/` contains the Workshop-facing UI source:
- Master Bundle metadata
- Effect asset metadata (default Effect ID `51000`)
- UI contract v1.0
- Unity Editor builder that programmatically creates the `Effect.prefab`
- design system and release instructions

Players open the main UI with `/kjmenu` or `/menu`.

The server-side implementation and handoff package are **source-complete / staging-ready**, but not production-certified. **The final `.masterbundle`, platform bundles and `.hash` are not generated by this repository itself.** Export those in Unity using the Unturned `Project.unitypackage` and Master Bundle Tool described in `workshop-ui/WORKSHOP_RELEASE.md`.

### UI release consistency (重要)

The server Core DLL, the client module and the Workshop Effect are one release unit. Do not mix a
new Core DLL with an older Workshop bundle: the Effect element names are an API contract, and a
version mismatch can produce a blank Mini Map, missing controls or stale arrows.

For every UI change, use this order:

1. Change the Unity builder/source and the matching server/client code together.
2. Export with Unity **2022.3.62f3**; never hand-edit `Effect.prefab` or `.masterbundle` files.
3. Copy all four exported UI files from `workshop-ui/UnityProject/WorkshopExport/` into
   `Kanomjeen_UI_Workshop/` as one set, preserving `MasterBundle.dat` and `Effects/.../Asset.dat`.
4. Build the server plugins and client module from the same checkout, then deploy the matching DLLs.
5. Upload the complete `Kanomjeen_UI_Workshop/` folder to the existing Workshop item, restart the
   server, and fully restart the client before testing.

Do not commit server runtime folders, logs, Unity `Library/`, or ad-hoc Workshop files. Keep the
release package at the six files listed in `AGENTS.md`; this prevents stale bundles and generated
runtime files from being mistaken for source changes during the next release.

## UI reliability notes
The Workshop UI is one Effect (`51000`) whose screens are switched by server-controlled visibility. Two rules keep it from rendering stacked screens: every `KJ_Screen_*` container must be listed in `UiService.Screens` (cross-checked by `verify-source.sh` / `verify-source.ps1`), and an explicit menu open always re-sends a fresh Effect instance while in-menu navigation only toggles visibility.

Details, call-site conventions, the full UI reliability checklist and the third-party reference implementations that were studied are documented in `AGENTS.md` §13.

## Build and validation
See:
- `RELEASE_MANIFEST.md`
- `BUILD.md`
- `INSTALL.md`
- `COMMANDS.md`
- `PERMISSIONS.md`
- `QA_REPORT.md`
- `THIRD_PARTY_NOTICES.md`
- `workshop-ui/WORKSHOP_RELEASE.md`
- `FINAL_LOCAL_PROMPT.md` — one complete prompt for the AI/agent on the real deployment computer
- `AGENTS.md` — working instructions for AI agents continuing this project (toolchain, export workflow, verification recipes, Workshop publishing)

A real server build/test is a mandatory release gate. This connected workspace does not expose the target server runtime or Unity Editor, so the repository must not be considered compile-certified or Workshop-published until `FINAL_LOCAL_PROMPT.md` is executed against the real deployment/staging environment and the critical gates in `QA_REPORT.md` pass.
